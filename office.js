import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.180.0/build/three.module.js';
const scene=new THREE.Scene();scene.background=new THREE.Color(0xc8e5ff);scene.fog=new THREE.Fog(0xc8e5ff,22,65);const camera=new THREE.PerspectiveCamera(60,innerWidth/innerHeight,.1,100);const renderer=new THREE.WebGLRenderer({antialias:false,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));renderer.setSize(innerWidth,innerHeight);document.querySelector('#game').appendChild(renderer.domElement);scene.add(new THREE.HemisphereLight(0xffffff,0x666666,2));const sun=new THREE.DirectionalLight(0xffffff,2);sun.position.set(10,20,10);scene.add(sun);
const mats={floor:new THREE.MeshLambertMaterial({color:0x777b82}),wall:new THREE.MeshLambertMaterial({color:0xd8dadd}),desk:new THREE.MeshLambertMaterial({color:0x8a5a35}),shirt:new THREE.MeshLambertMaterial({color:0x4676b9}),skin:new THREE.MeshLambertMaterial({color:0xe1a47f}),pants:new THREE.MeshLambertMaterial({color:0x242832}),screen:new THREE.MeshLambertMaterial({color:0x4db6d7}),plant:new THREE.MeshLambertMaterial({color:0x4d9a50})};const geo=new THREE.BoxGeometry(1,1,1);function cube(x,y,z,s,m){const o=new THREE.Mesh(geo,m);o.position.set(x,y,z);o.scale.set(s.x,s.y,s.z);scene.add(o)}
cube(0,-.5,0,{x:24,y:1,z:18},mats.floor);cube(-12,4,0,{x:1,y:9,z:18},mats.wall);cube(12,4,0,{x:1,y:9,z:18},mats.wall);cube(0,4,-9,{x:24,y:9,z:1},mats.wall);cube(0,8.5,0,{x:24,y:1,z:18},mats.wall);for(let x=-8;x<=8;x+=4)for(let z=-5;z<=5;z+=5){cube(x,1,z,{x:3,y:2,z:1.5},mats.desk);cube(x,2.25,z,{x:1.5,y:1,z:.12},mats.screen)}for(const p of [[-9,-7],[9,-7],[-9,7],[9,7]])cube(p[0],1,p[1],{x:1.2,y:2,z:1.2},mats.plant);
const worker=new THREE.Group();scene.add(worker);worker.position.set(-8,0,-5);function part(x,y,z,s,m){const o=new THREE.Mesh(geo,m);o.position.set(x,y,z);o.scale.set(...s);worker.add(o)}part(0,2.8,0,[.8,1,.55],mats.shirt);part(0,3.8,0,[.65,.7,.65],mats.skin);part(-.25,1.65,0,[.28,1.2,.35],mats.pants);part(.25,1.65,0,[.28,1.2,.35],mats.pants);part(-.62,2.65,0,[.2,.9,.2],mats.skin);part(.62,2.65,0,[.2,.9,.2],mats.skin);scene.add(camera);camera.position.set(0,9,14);camera.lookAt(0,2,0);
const path=[[-8,-5],[-8,0],[-8,5],[-4,5],[-4,0],[-4,-5],[0,-5],[0,0],[0,5],[4,5],[4,0],[4,-5],[8,-5],[8,0],[8,5],[-8,5]];let target=1,started=false,sim=0,last=performance.now();const $=id=>document.getElementById(id);$('play').onclick=()=>{started=true;$('start').style.display='none'};
function work(dt){const [tx,tz]=path[target],dx=tx-worker.position.x,dz=tz-worker.position.z,d=Math.hypot(dx,dz);if(d<.1){target=(target+1)%path.length;return}worker.position.x+=dx/d*1.7*dt;worker.position.z+=dz/d*1.7*dt;worker.rotation.y=Math.atan2(dx,dz);const n=target%5;$('task').textContent=n===0?'Mengambil dokumen':n===1?'Mengirim laporan':n===2?'Bekerja di komputer':n===3?'Rapat':'Istirahat singkat'}
function loop(t){const dt=Math.min((t-last)/1000,.05);last=t;if(started){sim+=dt;work(dt);const total=8*3600+sim*20,h=Math.floor(total/3600)%24,m=Math.floor(total/60)%60;$('clock').textContent=String(h).padStart(2,'0')+':'+String(m).padStart(2,'0');$('day').textContent=1+Math.floor(sim/180);$('money').textContent=100+Math.floor(sim/8)}renderer.render(scene,camera);requestAnimationFrame(loop)}requestAnimationFrame(loop);addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});